#!groovy

String buildShell = "$env.buildShell"

pipeline{
  agent { node { label "master"}}

  stages{
    stage("build"){
      steps{
        script{
          mvnHome = tool "m2"       
          println(buildShell)
          bat "$mvnHome\\bin\\mvn -v"
        }
      }
    }
  }
}
