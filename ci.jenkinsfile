pipeline{
  agent { node { label "master"}}

  stages{
    stage("build"){
      steps{
        script{
          mvnHome = tool "m2"
          println(mvnHome)
          println("This is test")
/*          bat "${mvnHome/bin/mvn -v}"
*/
        }
      }
    }
  }
}
